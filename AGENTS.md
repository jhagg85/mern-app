## Learned User Preferences
- Prefers error explanations that focus on root cause and prevention, not only the fix.
- Often asks for concise, practical debugging guidance tied to specific files or lines.

## Learned Workspace Facts
- Workspace is a MERN app split into `frontend` and `backend` directories.
- Frontend uses Vite + React with Tailwind CSS and daisyUI.
- Backend uses Node.js with ESM imports (`"type": "module"`), so local imports require `.js` extensions.
